# ─── Saubh.Tech Platform — Root Environment Variables ────────────────
# Copy to .env and fill in your values. NEVER commit .env.
# ─────────────────────────────────────────────────────────────────────

# ─── Web (apps/web) ─────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:3001/api
NEXT_PUBLIC_LANG_API_URL=/api/lang
LANG_API_INTERNAL_URL=http://localhost:3100

# ─── API (apps/api) ─────────────────────────────────────────────────
DATABASE_URL=postgresql://saubhtech:change_me_postgres_dev@127.0.0.1:5555/saubhtech?schema=public
PORT=3001
CORS_ORIGINS=http://localhost:3000

# ─── Docker dev stack (infra/compose/dev/) ───────────────────────────
# See infra/compose/dev/.env.dev.example for Docker-specific vars.
